define([
        'jquery'
    ],
    function ($) {
        'use strict';

        // This is an example spec. Delete it once you start writing real tests.
        describe('jQuery version', function () {
            it('should be 2.1.4', function () {
                expect($.fn.jquery).toBe('2.1.4');
            });

        });
    }
);
